# Godot 4+ specific ignores
.godot/
/android/

# Platform export output
/build/
*.ipa
*.apk
*.aab

# iOS export artifacts, if exported into the project root rather than /build
/puzzle.xcodeproj/
/puzzle/
/puzzle.pck
/*.xcframework/
/PrivacyInfo.xcprivacy

# Xcode export output. This lands in the project tree and reaches ~800MB
# (DerivedData, MoltenVK static libs, the .pck) -- never commit it.
/ios/

# Editor/agent tooling metadata
.serena/
.DS_Store
